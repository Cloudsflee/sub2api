{
  "name": "sub2api-product-sync-worker",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "test": "node --check index.js && node --check challenge-manager.js && node --check catalog-sync.js && node --check healthcheck.js && node --test *.test.js"
  },
  "dependencies": {
    "playwright-core": "1.61.1"
  }
}
